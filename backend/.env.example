# ----- Frontend origin (your Hostinger domain). Use * during development -----
FRONTEND_ORIGIN=https://tu-dominio.com

# ----- Firebase -----
FIREBASE_PROJECT_ID=tu-proyecto-firebase-id

# ----- HiveMQ / MQTT Broker -----
HIVEMQ_HOST=xxxxxxxx.s1.eu.hivemq.cloud
HIVEMQ_PORT=8883
HIVEMQ_USERNAME=usuario_mqtt
HIVEMQ_PASSWORD=contraseña_mqtt

# Use TLS
MQTT_TLS=1
# Set to 1 only if you must skip certificate validation (not recommended)
MQTT_TLS_INSECURE=0
# Optional path to a custom CA certificate file, usually leave empty
MQTT_CA_CERT_PATH=

# ----- Topic scoping -----
# All client traffic is scoped under: {TOPIC_BASE}/{uid}/...
TOPIC_BASE=scada/customers

# Optionally allow extra shared prefixes (comma separated), e.g. public/broadcast
PUBLIC_ALLOWED_PREFIXES=public/broadcast
